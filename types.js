// Declaring Variables

Immutable-ish
    const foo = 'bar'

Mutable
    var foo = 'bar' /* Hoisted */
    let foo = 'bar'

// Types

    // String
    str = 'foo' + bar
    str = `foo ${bar}`

    // Integer
    int = 42

    // Float
    float = 42.42

    // Boolean
    bool = true || false

    // Array []
    arr = ['a', 42, true, {}]

    // Object {}
    obj = { key: 'property' }
