
Immutable

    # String (str)
    str = 'foo'

    # Integer (int)
    int = 42

    # Float (float)
    float = 42.42

    # Boolean (bool)
    bool = True or False

    # Tuple (tuple)
    tuple = ('a', 42) or (42.42, True) or ([], {})

Mutable

    # List (list, [])
    list = ['a', 42, True, (), {}]

    # Dictionary (dict, {})
    dict = {"key": 'property'}

Falsy

    None